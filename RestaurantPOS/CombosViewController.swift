import UIKit

class CombosViewController: UIViewController {
    
    override func viewDidLoad() {
        super.viewDidLoad()
        self.title = "Combos"
    }
    
    @IBAction func combo1(_ sender: UIButton){
        let name = Notification.Name(rawValue: comboNumber1Key)
        NotificationCenter.default.post(name: name, object: nil)
    }
    @IBAction func combo2(_ sender: UIButton) {
        let name = Notification.Name(rawValue: comboNumber2Key)
        NotificationCenter.default.post(name: name, object: nil)
    }
    
    @IBAction func combo3(_ sender: UIButton) {
        let name = Notification.Name(rawValue: comboNumber3Key)
        NotificationCenter.default.post(name: name, object: nil)
    }
    
    @IBAction func combo4(_ sender: UIButton) {
        let name = Notification.Name(rawValue: comboNumber4Key)
        NotificationCenter.default.post(name: name, object: nil)
    }
    
    @IBAction func combo5(_ sender: UIButton) {
        let name = Notification.Name(rawValue: comboNumber5Key)
        NotificationCenter.default.post(name: name, object: nil)
    }
    
    @IBAction func combo6(_ sender: UIButton) {
        let name = Notification.Name(rawValue: comboNumber6Key)
        NotificationCenter.default.post(name: name, object: nil)
    }
    
    @IBAction func combo7(_ sender: UIButton) {
        let name = Notification.Name(rawValue: comboNumber7Key)
        NotificationCenter.default.post(name: name, object: nil)
    }
    
    @IBAction func combo8(_ sender: UIButton) {
        let name = Notification.Name(rawValue: comboNumber8Key)
        NotificationCenter.default.post(name: name, object: nil)
    }
    
    @IBAction func combo9(_ sender: UIButton) {
        let name = Notification.Name(rawValue: comboNumber9Key)
        NotificationCenter.default.post(name: name, object: nil)
    }
    
    @IBAction func combo10(_ sender: UIButton) {
        let name = Notification.Name(rawValue: comboNumber10Key)
        NotificationCenter.default.post(name: name, object: nil)
    }
    
    @IBAction func combo11(_ sender: UIButton) {
        let name = Notification.Name(rawValue: comboNumber11Key)
        NotificationCenter.default.post(name: name, object: nil)
    }
    
    @IBAction func combo12(_ sender: UIButton) {
        let name = Notification.Name(rawValue: comboNumber12Key)
        NotificationCenter.default.post(name: name, object: nil)
    }
    
    @IBAction func combo14(_ sender: UIButton) {
        let name = Notification.Name(rawValue: comboNumber14Key)
        NotificationCenter.default.post(name: name, object: nil)
    }
    
    @IBAction func combo15(_ sender: UIButton) {
        let name = Notification.Name(rawValue: comboNumber15Key)
        NotificationCenter.default.post(name: name, object: nil)
    }
    
    @IBAction func combo16(_ sender: UIButton) {
        let name = Notification.Name(rawValue: comboNumber16Key)
        NotificationCenter.default.post(name: name, object: nil)
    }
    
    @IBAction func combo17(_ sender: UIButton) {
        let name = Notification.Name(rawValue: comboNumber17Key)
        NotificationCenter.default.post(name: name, object: nil)
    }
    
    @IBAction func combo18(_ sender: UIButton) {
        let name = Notification.Name(rawValue: comboNumber18Key)
        NotificationCenter.default.post(name: name, object: nil)
    }
    
    @IBAction func combo19(_ sender: UIButton) {
        let name = Notification.Name(rawValue: comboNumber19Key)
        NotificationCenter.default.post(name: name, object: nil)
    }
}
